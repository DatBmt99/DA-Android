import 'package:flutter/material.dart';

class ChangeLgScreen extends StatefulWidget {
  const ChangeLgScreen({
    Key key,
  }) : super(key: key);
  @override
  _ChangeLgScreenState createState() => _ChangeLgScreenState();
}

class _ChangeLgScreenState extends State<ChangeLgScreen> {
  // void _changeLanguage(Language language) {
  //   Locale _temp;
  //   switch(language)
  // }

  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;

    return Scaffold();
  }
}
